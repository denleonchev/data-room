# Server
PORT=4000
CORS_ORIGIN=http://localhost:5173

# Database (Supabase Postgres). DATABASE_URL is the pooled connection (used at
# runtime), DIRECT_URL is the non-pooled connection Prisma needs for migrations.
DATABASE_URL=
DIRECT_URL=
